@{
    ViewData["Title"] = "Home";
}

<div class="container-fluid">
    <!-- Hero Section -->
    <div class="row justify-content-center text-center">
        <div class="col-lg-10">
            <div class="hero-section p-5 rounded-4 bg-primary text-white">
                <h1 class="display-4 fw-bold mb-3">
                    <i class="fas fa-building-columns me-3"></i>
                    Company Management System
                </h1>
                <p class="lead mb-4">
                    Streamline your organizational structure with our comprehensive department management solution
                </p>
                <div class="d-flex justify-content-center gap-3">
                    <a href="@Url.Action("Index", "Department")" class="btn btn-light btn-lg">
                        <i class="fas fa-eye me-2"></i>
                        View Departments
                    </a>
                    <a href="@Url.Action("Create", "Department")" class="btn btn-outline-light btn-lg">
                        <i class="fas fa-plus-circle me-2"></i>
                        Create Department
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.hero-section {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    margin-top: 2rem;
    margin-bottom: 2rem;
}
</style> 