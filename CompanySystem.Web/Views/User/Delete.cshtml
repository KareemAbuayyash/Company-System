@model CompanySystem.Web.ViewModels.UserViewModel
@{
    ViewData["Title"] = "Delete User";
    Layout = "_Layout";
}

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        Delete User
                    </h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <h5 class="alert-heading">
                            <i class="fas fa-exclamation-triangle me-1"></i>
                            Warning!
                        </h5>
                        <p class="mb-0">Are you sure you want to delete this user? This action cannot be undone.</p>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <h6 class="text-primary">Personal Information</h6>
                            <dl class="row">
                                <dt class="col-sm-4">Name</dt>
                                <dd class="col-sm-8">@Model.FullName</dd>

                                <dt class="col-sm-4">Username</dt>
                                <dd class="col-sm-8">@Model.Username</dd>

                                <dt class="col-sm-4">Email</dt>
                                <dd class="col-sm-8">@Model.Email</dd>

                                <dt class="col-sm-4">Phone</dt>
                                <dd class="col-sm-8">
                                    @if (!string.IsNullOrEmpty(Model.PhoneNumber))
                                    {
                                        @Model.PhoneNumber
                                    }
                                    else
                                    {
                                        <span class="text-muted">Not provided</span>
                                    }
                                </dd>
                            </dl>
                        </div>
                        <div class="col-md-6">
                            <h6 class="text-primary">Account Information</h6>
                            <dl class="row">
                                <dt class="col-sm-4">Role</dt>
                                <dd class="col-sm-8">
                                    <span class="badge bg-info">@Model.RoleName</span>
                                </dd>

                                <dt class="col-sm-4">Department</dt>
                                <dd class="col-sm-8">
                                    @if (!string.IsNullOrEmpty(Model.DepartmentName))
                                    {
                                        <span class="badge bg-secondary">@Model.DepartmentName</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">Not assigned</span>
                                    }
                                </dd>

                                <dt class="col-sm-4">Status</dt>
                                <dd class="col-sm-8">
                                    @if (Model.IsActive)
                                    {
                                        <span class="badge bg-success">Active</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">Inactive</span>
                                    }
                                </dd>

                                <dt class="col-sm-4">Created</dt>
                                <dd class="col-sm-8">
                                    <small>@Model.CreatedDate.ToLocalTime().ToString("dd/MM/yyyy")</small>
                                    <br>
                                    <small class="text-muted">by @Model.CreatedBy</small>
                                </dd>
                            </dl>
                        </div>
                    </div>

                    <form asp-action="Delete" method="post">
                        <input type="hidden" name="id" value="@Model.UserId" />
                        
                        <div class="d-flex justify-content-end mt-4">
                            <a href="@Url.Action("Index", "User")" class="btn btn-secondary me-2">
                                <i class="fas fa-arrow-left me-1"></i>
                                Cancel
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="fas fa-trash me-1"></i>
                                Delete User
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

